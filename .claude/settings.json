{
  "permissions": {
    "allow": [
      "Bash(cmake --build . --target test_ms_generate -j 8)",
      "Bash(ctest -R \"PreferenceGeneration\" --output-on-failure)",
      "Bash(cmake --build . -j 8)",
      "Bash(ctest --output-on-failure)"
    ]
  }
}
